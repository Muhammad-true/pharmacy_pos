# Чеклист подготовки к релизу

## ✅ Выполнено

### 1. Логирование
- ✅ Заменены все `print()` на `ErrorHandler` в критичных местах:
  - `lib/core/config/app_config.dart` - конфигурация
  - `lib/core/database/database.dart` - подключение к БД
- ✅ Используется централизованный `ErrorHandler` для всех ошибок

### 2. Тестовые данные
- ✅ Тестовые данные в `api_service.dart` теперь контролируются флагом `ENABLE_MOCK_DATA`
- ✅ По умолчанию тестовые данные отключены (безопасно для production)
- ✅ Для включения тестовых данных нужно добавить в `.env`: `ENABLE_MOCK_DATA=true`

### 3. Конфигурация
- ✅ `debugShowCheckedModeBanner: false` во всех MaterialApp
- ✅ Версия приложения: `1.0.0+1` в `pubspec.yaml`

### 4. Безопасность
- ✅ Нет хардкодных паролей в коде (используется `.env` файл)
- ✅ Чувствительные данные загружаются из переменных окружения

## ⚠️ Требует внимания

### 1. TODO комментарии
Оставлены следующие TODO (не критично, но стоит учесть):
- `lib/services/api/api_service.dart:12` - Заменить на реальный базовый URL (если планируется внешний API)
- `lib/services/api/api_service.dart:198, 256` - Убрать моковые данные (уже защищено флагом)
- `lib/features/warehouse/screens/warehouse_screen.dart:885` - Открыть детальную информацию о товаре

### 2. Отладочные print() в UI
✅ **ВЫПОЛНЕНО:** Все `print()` в UI файлах обернуты в `kDebugMode`:
- ✅ `lib/features/cashier/widgets/discount_dialog.dart`
- ✅ `lib/features/cashier/screens/cashier_screen.dart`
- ✅ `lib/features/cashier/widgets/product_search_field.dart`
- ✅ `lib/features/cashier/widgets/calculation_panel.dart`
- ✅ `lib/features/client/screens/client_screen.dart`
- ✅ `lib/services/client_window_service.dart` (частично - остались некоторые для диагностики)
- ⚠️ `lib/core/database/mysql_executor.dart` (контролируется флагом `logStatements` - можно оставить)
- ⚠️ `lib/core/repositories/receipt_repository.dart` (несколько print() для диагностики)

**Статус:** Все критичные print() в UI обернуты в `kDebugMode`. В production они не будут выполняться.

### 3. Конфигурация для релиза

#### .env файл для production:
```env
# Тип базы данных
DATABASE_TYPE=mysql

# Настройки MySQL
MYSQL_HOST=your_host
MYSQL_PORT=3306
MYSQL_USER=your_user
MYSQL_PASSWORD=your_password
MYSQL_DATABASE=pharmacy_db

# Отключить тестовые данные (важно!)
ENABLE_MOCK_DATA=false

# Настройки приложения
APP_NAME=Аптека Хушдил - POS
APP_VERSION=1.0.0
LOG_LEVEL=info
ENVIRONMENT=production
```

## 📋 Дополнительные проверки перед релизом

### 1. Тестирование
- [ ] Протестировать все основные функции:
  - [ ] Вход в систему (все роли)
  - [ ] Работа кассира (создание чеков, оплата)
  - [ ] Работа склада (добавление товаров, пополнение)
  - [ ] Работа администратора (управление пользователями, статистика)
  - [ ] Окно клиента (отображение чека)
- [ ] Протестировать на разных разрешениях экрана
- [ ] Протестировать темную и светлую темы
- [ ] Протестировать локализацию (все языки)

### 2. Производительность
- [ ] Проверить время загрузки приложения
- [ ] Проверить работу с большим количеством товаров
- [ ] Проверить работу с большим количеством чеков
- [ ] Проверить использование памяти

### 3. Безопасность
- [ ] Убедиться, что `.env` файл не включен в репозиторий (должен быть в `.gitignore`)
- [ ] Проверить, что пароли не логируются
- [ ] Проверить валидацию входных данных
- [ ] Проверить обработку ошибок (нет утечки информации)

### 4. Документация
- [ ] Обновить README.md с инструкциями по установке
- [ ] Создать инструкцию по настройке MySQL
- [ ] Создать инструкцию по настройке .env файла
- [ ] Документировать процесс развертывания

### 5. Сборка релиза
```bash
# Для Windows
flutter build windows --release

# Проверить размер приложения
# Проверить, что все ресурсы включены
```

## ✅ Выполненные улучшения

### Профессиональная подготовка к релизу:
1. ✅ Все критичные `print()` заменены на `ErrorHandler` или обернуты в `kDebugMode`
2. ✅ Тестовые данные контролируются флагом `ENABLE_MOCK_DATA` (по умолчанию отключены)
3. ✅ Централизованное логирование через `ErrorHandler`
4. ✅ Безопасность: нет хардкодных паролей, все через `.env`
5. ✅ `debugShowCheckedModeBanner: false` во всех MaterialApp
6. ✅ Версия приложения установлена: `1.0.0+1`

## 🚀 Готово к релизу

После выполнения всех проверок приложение готово к релизу!

**Важно:** Убедитесь, что в production `.env` файле установлено:
- `ENABLE_MOCK_DATA=false` (или не указано)
- Правильные настройки MySQL
- `ENVIRONMENT=production`

## 📝 Дополнительные рекомендации

1. **Сборка релиза:**
   ```bash
   flutter clean
   flutter pub get
   flutter build windows --release
   ```

2. **Проверка размера:** Убедитесь, что размер приложения приемлемый

3. **Тестирование:** Проведите полное тестирование всех функций перед релизом

4. **Резервное копирование:** Сохраните резервную копию базы данных перед обновлением

